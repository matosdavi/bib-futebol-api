services:
  db:
    image: mysql:8.0
    container_name: bib-mysql-container
    restart: always
    environment:
      MYSQL_DATABASE: bib_database
      MYSQL_ROOT_PASSWORD: rootpassword
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: